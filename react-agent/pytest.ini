[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -ra -q --tb=short
markers =
    integration: requires real API keys (GEMINI_API_KEY etc.)
    unit: no external dependencies
